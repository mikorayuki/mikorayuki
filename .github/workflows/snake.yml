name: Generate Snake 🐍

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Snake SVG
        uses: Platane/snk@v2.1.1
        with:
          github_user_name: mikorayuki
          outputs: |
            dist/github-contribution-grid-snake.svg
      - name: Push to GitHub Pages
        uses: crazy-max/ghaction-g
